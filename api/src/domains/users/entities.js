export class User {
    constructor({email, firstName, lastName, role}){
        this.email = email,
        this.firstName = firstName,
        this.lastName = lastName,
        this.role = role
    }
}